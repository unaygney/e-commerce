export function adjustSize(size: string) {
  if (size === "xs") return "XS";
  if (size === "sm") return "S";
  if (size === "md") return "M";
  if (size === "lg") return "L";
  if (size === "xl") return "XL";
}
